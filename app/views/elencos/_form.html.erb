<%= form_for(elenco) do |f| %>
  <% if elenco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(elenco.errors.count, "error") %> prohibited this elenco from being saved:</h2>

      <ul>
      <% elenco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_filme %>
    <%= f.text_field :id_filme %>
  </div>

  <div class="field">
    <%= f.label :id_ator %>
    <%= f.text_field :id_ator %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
